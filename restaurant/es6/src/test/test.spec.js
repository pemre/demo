/*var assert = require('assert');
describe('Array', function() {
    describe('#indexOf()', function() {
        it('should return -1 when the value is not present', function() {
            assert.equal(-1, [1,2,3].indexOf(4));
        });
    });
});*/

// Import the Rectangle class.
import RestaurantService from '../js/services/restaurant.service'
//let RestaurantService = require('../js/services/restaurant.service');

/*describe('RestaurantService', () => {
    describe('get all data', () => {
        let r;

        beforeEach(() => {
            // Create a new Rectangle object before every test.
            r = new RestaurantService();
        });

        it('returns rests', () => {
            // This will fail if "rectangle.width" does
            // not equal 10.
            r.getFavouriteRestaurants().should.equal([]);
        });
    });
});*/

describe('A test suite', function() {
    it('should fail', function() {
        expect(true).to.be.true;

    });
});